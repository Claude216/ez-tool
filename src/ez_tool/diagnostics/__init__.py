"""Feasibility diagnostics for a CNB tool retriever on ToolBench.

Statistics only. Nothing here trains a classifier, builds an index, or
imports ``ezr.py``; :mod:`ez_tool.diagnostics.textpipe` re-implements EZR's
text pipeline from its source so the numbers describe EZR's behaviour without
inheriting its globals.
"""
